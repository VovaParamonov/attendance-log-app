import {StyleSheet} from "react-native";

export default StyleSheet.create({
    select_wrapper: {
        borderBottomColor: "#030706",
        borderBottomWidth: 1,
        position: "relative",
        paddingTop: 10,
        marginBottom: 15
    },

    select: {
    },

    select_item: {
        // fontSize: 5
    },

    select_title: {
        position: "absolute",
        left: 0,
        top: 0,
        fontSize: 10,
        opacity: .5,
    }
});